import React from 'react';

const ConcertFormDesktop = ({ id = null, onClose, onSave, defaultView = 'form', isEmbedded = false }) => {
  return (
    <div className="concert-form-desktop">
      {/* ... existing code ... */}
    </div>
  );
};

export default ConcertFormDesktop; 