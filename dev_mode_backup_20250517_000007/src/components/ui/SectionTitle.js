import React from 'react';

import styles from './SectionTitle.module.css';

const SectionTitle = (props) => {
  return <div className={styles.container}>SectionTitle Placeholder</div>;
};

export default SectionTitle;
